<div class= "media">
  <div class="media-body">
    <small>
      <%= comment.user.name %> commented <%= time_ago_in_words(comment.created_at) %> time_ago_in_words
    </small>
    <p><%= comment.body %></p>
  </div>
</div>